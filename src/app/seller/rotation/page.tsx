import ProductRotationManager from '@/components/ProductRotationManager';

export const metadata = { title: 'Rotación — Makay' };

export default function SellerRotationPage() {
  return <ProductRotationManager />;
}
