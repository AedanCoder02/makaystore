import ProductGenerationWizard from '@/components/ProductGenerationWizard';

export const metadata = { title: 'Generate 3D Model — Seller' };

export default function SellerCreate3DPage() {
  return <ProductGenerationWizard />;
}
